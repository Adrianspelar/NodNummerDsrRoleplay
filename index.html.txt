<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>112 - SOS Alarm</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            text-align: center;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        .btn {
            background-color: #ff4d4d;
            color: white;
            border: none;
            padding: 20px 40px;
            font-size: 20px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #ff3333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>112 - SOS Alarm</h1>
        <p>Tryck på knappen för att ringa SOS och prata med en operatör.</p>
        <button class="btn" id="callButton">Ring 112</button>
    </div>

    <script src="https://meet.jit.si/external_api.js"></script>
    <script>
        document.getElementById("callButton").onclick = function() {
            const roomName = "112-SOS-Dalarna-" + Math.floor(Math.random() * 1000); // Random room name
            const domain = "meet.jit.si";
            const options = {
                roomName: roomName,
                width: '100%',
                height: '100%',
                parentNode: document.body
            };
            const api = new JitsiMeetExternalAPI(domain, options);
        };
    </script>
</body>
</html>